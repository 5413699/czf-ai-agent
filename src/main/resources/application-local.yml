spring:
  ai:
    openai:
      base-url: https://dashscope.aliyuncs.com/compatible-mode/v1
      # 必须提供环境变量，缺失时使用默认值（方便演示）
      # 实际项目中，建议api-key: ${DASHSCOPE_API_KEY}，缺失时直接报错
      api-key: ${DASHSCOPE_API_KEY}
      chat:
        model: qwen-plus